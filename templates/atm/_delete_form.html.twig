<form method="post" action="{{ path('app_atm_delete', {'id': atm.id}) }}" onsubmit="return confirm(Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ atm.id) }}">
    <button class="btn">Delete</button>
</form>
