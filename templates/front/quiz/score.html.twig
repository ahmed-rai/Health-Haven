{% extends 'base.html.twig' %}

{% block title %}Quiz Score{% endblock %}

{% block body %}
    <h1>Your Quiz Score</h1>
    <h2>Quiz: {{ quiz.name }}</h2>
    <p>
        Your score is <strong>{{ score }}</strong> out of <strong>{{ max_score }}</strong>.
    </p>
    <p>
        {% if score >= threshold %}
            Based on your score, you may be experiencing some issues. We recommend you to consult a professional.
        {% else %}
            Based on your score, you seem to be doing well. Keep up the good work!
        {% endif %}
    </p>

    <a href="{{ path('quiz_selection') }}" class="btn btn-primary">Take another quiz</a>
{% endblock %}
