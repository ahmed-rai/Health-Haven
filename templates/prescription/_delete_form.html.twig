<form method="post" action="{{ path('app_prescription_delete', {'dosage': prescription.dosage}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prescription.dosage) }}">
    <button class="btn">Delete</button>
</form>
