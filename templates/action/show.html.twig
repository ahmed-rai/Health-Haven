{% extends 'back.html.twig' %}

{% block title %}Action{% endblock %}

{% block body %}
    <h1>Action</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ action.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ action.nom }}</td>
            </tr>
            <tr>
                <th>Lieuact</th>
                <td>{{ action.lieuact }}</td>
            </tr>
            <tr>
                <th>Dteact</th>
                <td>{{ action.dteact }}</td>
            </tr>
            <tr>
                <th>Hract</th>
                <td>{{ action.hract }}</td>
            </tr>
            <tr>
                <th>Descract</th>
                <td>{{ action.descract }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_action_indexBack') }}">back to list</a>

    <a href="{{ path('app_action_edit', {'id': action.id}) }}">edit</a>

    {{ include('action/_delete_form.html.twig') }}
{% endblock %}
